# ✅ ЧЕКЛИСТ ДЕПЛОЯ И ПРОВЕРКИ API

## 🎯 Цель

Убедиться, что формы обратной связи работают на **zol-dub.online** после деплоя.

---

## 📋 Перед деплоем

### 1. Проверить локально

```bash
# Запустить dev-сервер
npm run dev

# В другом терминале запустить тесты
./test-api-contact.sh http://localhost:3000

# Ожидается:
# ✅ GET запрос успешен (HTTP 200)
# ✅ Валидация работает (HTTP 400)
# ✅ POST запрос успешен (HTTP 200 или 500)
```

### 2. Проверить структуру файлов

```bash
# API route существует
ls -la app/api/contact/route.ts

# Формы существуют
ls -la app/contacts/ContactForm.tsx
ls -la app/components/CalculationModal.tsx

# Server action существует
ls -la app/contacts/actions.ts
```

### 3. Проверить переменные окружения

**Локально (`.env.local`):**
```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHI...
TELEGRAM_CHAT_ID=277767867,956005680
```

**На Vercel:**
- Settings → Environment Variables
- Проверить наличие обеих переменных
- Применить к Production, Preview, Development

---

## 🚀 Деплой

### Способ 1: Git Push (автоматический)

```bash
git add .
git commit -m "feat: add API fallback for contact forms"
git push origin main
```

### Способ 2: Vercel CLI

```bash
vercel --prod
```

---

## 🔍 После деплоя

### 1. Проверить деплой в Vercel Dashboard

```
✅ Build завершен успешно
✅ Deployment Status: Ready
✅ Functions deployed: 3
    - /api/contact
    - /api/images
    - /api/telegram-webhook
```

### 2. Тестирование API endpoint

```bash
# Тест на production
./test-api-contact.sh https://zol-dub.online

# Ожидается:
# ✅ Endpoint существует и отвечает
# ✅ Валидация входных данных работает
# ✅ Отправка данных работает полностью
```

### 3. Проверить в браузере

**Шаг 1: Открыть GET endpoint**
```
https://zol-dub.online/api/contact
```

**Ожидается:**
```json
{
  "status": "ok",
  "message": "Contact API endpoint is working",
  "methods": ["POST"],
  "usage": "Send POST request with { name, phone?, email?, message? }",
  "timestamp": "2025-11-05T..."
}
```

**Если видите 404:**
- ❌ Файл не задеплоился
- Проверьте Build Logs в Vercel
- Убедитесь что `app/api/contact/route.ts` в git

---

### 4. Проверить отправку формы

**Шаг 1: Открыть сайт**
```
https://zol-dub.online
```

**Шаг 2: Открыть DevTools (F12)**
```
Console вкладка
```

**Шаг 3: Перейти на страницу контактов**
```
https://zol-dub.online/contacts
```

**Шаг 4: Заполнить и отправить форму**

**Ожидается в Console:**
```
═════════════════════════════════════════════════════════
📝 [ContactForm] FORM SUBMISSION STARTED
═════════════════════════════════════════════════════════
[ContactForm] Timestamp: 2025-11-05T...
[ContactForm] 📋 Form data extracted:
  - Name: Иван Петров
  - Phone: +7 930 123 45 67
  ...
[ContactForm] 🚀 Calling submitContactForm server action...
[ContactForm] ✅ Request completed in XXXms
[ContactForm] ✅ SUCCESS: Form submitted successfully
```

**Если видите fallback:**
```
[ContactForm] ⚠️ Server action failed, trying fallback API...
[ContactForm] 📡 Sending direct POST to /api/contact...
[ContactForm] ✅ Fallback API succeeded
```
→ Это нормально! Fallback сработал, форма отправлена.

**Если видите ошибку:**
```
[ContactForm] ❌ EXCEPTION CAUGHT
[ContactForm] Error message: ...
```
→ Скопируйте ошибку и проверьте логи Vercel

---

### 5. Проверить Vercel Runtime Logs

```
Vercel Dashboard → Project → Logs → Runtime Logs
```

**Ищите:**
```
[SERVER ACTION] submitContactForm CALLED
или
[API /contact] NEW REQUEST RECEIVED
```

**Если есть:**
```
[Telegram] ✅ SUCCESS! Message delivered to chat 277767867
```
→ Заявка дошла до Telegram ✅

**Если есть:**
```
[Telegram] ❌ API Error for chat ...
```
→ Проблема с Telegram (проверьте токен и chat_id)

---

## 🐛 Troubleshooting

### Проблема 1: 404 на /api/contact

**Симптомы:**
```bash
curl https://zol-dub.online/api/contact
# Ответ: 404 Not Found
```

**Решение:**
1. Проверить Build Logs в Vercel:
   ```
   Vercel Dashboard → Deployments → Latest → Build Logs
   ```
2. Убедиться что файл в git:
   ```bash
   git ls-files | grep "app/api/contact/route.ts"
   ```
3. Пересобрать и задеплоить:
   ```bash
   vercel --prod --force
   ```

---

### Проблема 2: Server Action не работает, fallback срабатывает

**Симптомы:**
```
[ContactForm] ⚠️ Server action failed, trying fallback API...
[ContactForm] ✅ Fallback API succeeded
```

**Это нормально!** Fallback создан именно для этого случая.

**Если хотите исправить Server Action:**
1. Проверьте Node.js version в Vercel:
   ```
   Settings → General → Node.js Version
   Рекомендуется: 18.x или 20.x
   ```
2. Проверьте что в `package.json`:
   ```json
   "engines": {
     "node": ">=18.0.0"
   }
   ```

---

### Проблема 3: Telegram не получает сообщения

**Симптомы:**
```
[API] ❌ CONFIG ERROR: Environment variables missing
```

**Решение:**
1. Проверить переменные в Vercel:
   ```
   Settings → Environment Variables
   
   TELEGRAM_BOT_TOKEN → ✓ Present
   TELEGRAM_CHAT_ID → ✓ Present
   ```
2. Убедиться что применены к Production:
   ```
   Environment: Production ✓
   ```
3. Пересобрать проект после изменения env vars:
   ```
   Deployments → Latest → ... → Redeploy
   ```

---

### Проблема 4: Форма отправляется, но нет в Telegram

**Симптомы:**
```
[ContactForm] ✅ SUCCESS: Form submitted successfully
```
Но в Telegram ничего не пришло.

**Проверить:**
1. Runtime Logs в Vercel:
   ```
   [Telegram] ✅ SUCCESS! Message delivered
   ```
   Если есть - сообщение отправлено, проверьте Telegram chat_id
   
2. Chat ID правильный?
   ```bash
   # Получить свой chat_id через бота @userinfobot
   # Отправьте ему /start
   ```

3. Бот добавлен в группу (если используется group chat_id)?
   ```
   Telegram → Группа → Добавить участников → Найти бота
   ```

---

## 📊 Ожидаемые результаты

### ✅ Все работает идеально

```
Пользователь → Заполняет форму → Нажимает "Отправить"
     ↓
Server Action → sendContactFormToTelegram()
     ↓
Telegram API → Сообщение доставлено
     ↓
Пользователь видит: "Заявка отправлена!"
```

**В логах:**
- Браузер: `[ContactForm] ✅ SUCCESS`
- Vercel: `[Telegram] ✅ SUCCESS!`
- Telegram: Сообщение получено

---

### ⚠️ Работает через fallback

```
Пользователь → Заполняет форму → Нажимает "Отправить"
     ↓
Server Action (FAIL)
     ↓
⚠️ Fallback: POST /api/contact
     ↓
Telegram API → Сообщение доставлено
     ↓
Пользователь видит: "Заявка отправлена!"
```

**В логах:**
- Браузер: `[ContactForm] ⚠️ Server action failed, trying fallback API...`
- Браузер: `[ContactForm] ✅ Fallback API succeeded`
- Vercel: `[API /contact] NEW REQUEST RECEIVED`
- Vercel: `[API] ✅ Successfully sent to 277767867`
- Telegram: Сообщение получено

---

### ❌ Не работает

```
Пользователь → Заполняет форму → Нажимает "Отправить"
     ↓
Server Action (FAIL)
     ↓
⚠️ Fallback: POST /api/contact (FAIL)
     ↓
Пользователь видит: "Произошла ошибка"
```

**В логах:**
- Браузер: `[ContactForm] ❌ EXCEPTION CAUGHT`
- Браузер: Error details
- Vercel: Errors или вообще нет логов

**Действия:**
1. Скопировать ошибку из Console
2. Проверить Vercel Runtime Logs
3. Проверить Environment Variables
4. Запустить тест локально

---

## 🔧 Быстрая диагностика (за 2 минуты)

### 1. Endpoint существует?
```bash
curl https://zol-dub.online/api/contact
# Ожидается: 200 OK с JSON
```
✅ Да → Идем дальше  
❌ 404 → Проблема с деплоем файла

### 2. POST работает?
```bash
curl -X POST https://zol-dub.online/api/contact \
  -H "Content-Type: application/json" \
  -d '{"name":"Test"}'
# Ожидается: 200 OK или 500 (но не 404!)
```
✅ 200/500 → API работает  
❌ 404 → Проблема с routing

### 3. Форма на сайте работает?
```
1. Открыть zol-dub.online
2. Открыть Console (F12)
3. Отправить форму
4. Смотреть логи
```
✅ Видно логи → Фронтенд работает  
❌ Нет логов → Проблема с JS

### 4. Telegram получает?
```
Проверить Telegram чат
```
✅ Есть сообщение → Все ОК  
❌ Нет → Проверить Vercel Logs

---

## 📝 Финальный чеклист

Перед тем как считать деплой завершенным:

- [ ] ✅ Build в Vercel успешен
- [ ] ✅ `/api/contact` возвращает 200 на GET
- [ ] ✅ POST с данными работает (200 или 500)
- [ ] ✅ Форма на сайте отправляется без ошибок
- [ ] ✅ Сообщения приходят в Telegram
- [ ] ✅ Логи в Vercel показывают успешную отправку
- [ ] ✅ В Console браузера нет красных ошибок
- [ ] ✅ Fallback работает (тестировали)
- [ ] ✅ Environment variables настроены
- [ ] ✅ Калькулятор тоже отправляет формы

---

## 🎉 Успех!

Если все пункты выполнены:

```
✅ API endpoint работает
✅ Формы отправляются
✅ Telegram получает заявки
✅ Fallback настроен на случай проблем
✅ Полное логирование для диагностики
```

**Сайт готов к приему заявок!** 🚀

---

**Последнее обновление:** 5 ноября 2025  
**Версия:** 1.0

