# ⚡ Быстрый деплой на ispmanager

## 🎯 ЗА 5 МИНУТ

### Шаг 1: Подготовка (на вашем компьютере)

```bash
cd ~/Desktop/zdub/zolotoy-dub

# Скопировать конфиг для static export
cp next.config.static.js next.config.js

# Запустить автоматический деплой
./deploy-static.sh
```

**Результат:**
- ✅ Создан бэкап: `zolotoy-dub-backup-*.tar.gz`
- ✅ Создан архив для загрузки: `zolotoy-dub-deploy-*.tar.gz`
- ✅ Готов к загрузке на сервер

---

### Шаг 2: Загрузка на ispmanager

#### Вариант A: Через File Manager (проще)

1. **Войти в ispmanager**
   - Откройте панель управления
   - Перейдите в "Файловый менеджер"

2. **Подготовить директорию**
   ```
   public_html/
   ├── backup_old/     ← создайте эту папку
   └── (старые файлы)  ← переместите в backup_old/
   ```

3. **Загрузить архив**
   - Нажмите "Загрузить"
   - Выберите `zolotoy-dub-deploy-*.tar.gz`
   - Дождитесь загрузки

4. **Распаковать**
   - Правой кнопкой на архив
   - "Извлечь" → в `public_html/`
   - Готово! ✅

#### Вариант B: Через FTP (FileZilla)

1. **Подключиться к FTP**
   - Host: ftp.your-domain.com
   - Username: ваш логин
   - Password: ваш пароль
   - Port: 21

2. **Перейти в public_html/**

3. **Сделать бэкап**
   - Выделить все старые файлы
   - Скачать на компьютер (или переместить в backup_old/)

4. **Загрузить новые файлы**
   - Перейти в папку `out/` на компьютере
   - Выделить всё
   - Загрузить в `public_html/`

5. **Проверить .htaccess**
   - Убедиться что файл `.htaccess` загружен
   - Если нет - загрузить вручную из `out/.htaccess`

---

### Шаг 3: Проверка работы

Откройте сайт: `https://your-domain.com`

**Проверьте:**
- [ ] Главная страница открывается
- [ ] Все страницы работают (каталог, контакты)
- [ ] Картинки загружаются
- [ ] Стили применены
- [ ] Мобильная версия OK

---

## ⚠️ ВАЖНО: Контактная форма

**Static Export НЕ поддерживает API routes!**

### Решение: PHP fallback

1. **Загрузите файл**
   ```
   contact-php-fallback.php → public_html/api/contact.php
   ```

2. **Настройте переменные**
   Откройте `contact.php` и укажите:
   ```php
   $botToken = '8397994876:AAHpHKfsdPrEvrGAgIVFGwoOKf6Uw1CPMak';
   $chatIds = ['277767867', '956005680'];
   ```

3. **Проверьте права**
   ```
   chmod 644 api/contact.php
   ```

4. **Тест**
   ```
   https://your-domain.com/api/contact.php
   ```

---

## 🔧 Если что-то не работает

### "500 Internal Server Error"

**Причина:** Синтаксис .htaccess  
**Решение:**
1. Проверить права: `chmod 644 .htaccess`
2. Проверить синтаксис
3. Отключить mod_rewrite если не поддерживается

### "404 Not Found на страницах"

**Причина:** Не работает rewrite  
**Решение:**
1. Убедиться что mod_rewrite включен
2. Проверить .htaccess
3. Добавить в начало `.htaccess`:
   ```apache
   RewriteBase /
   ```

### "Картинки не загружаются"

**Причина:** Неверные пути  
**Решение:**
1. Проверить что папка `images/` загружена
2. Проверить что `_next/static/` загружена
3. Проверить права: `chmod 755` на папки

### "Форма не отправляется"

**Причина:** API route не работает  
**Решение:**
1. Использовать PHP fallback (см. выше)
2. Или настроить внешний сервис (Formspree, EmailJS)

---

## 📊 Структура файлов на сервере

```
public_html/
├── .htaccess           ← Правила Apache
├── index.html          ← Главная страница
├── catalog.html        ← Каталог
├── contacts.html       ← Контакты
├── _next/              ← Ресурсы Next.js
│   └── static/
│       ├── chunks/
│       ├── css/
│       └── media/
├── images/             ← Картинки
│   └── kitchen-*.jpg
├── fonts/              ← Шрифты
└── api/                ← PHP скрипты (если нужно)
    └── contact.php
```

---

## ✅ ЧЕКЛИСТ ПОСЛЕ ДЕПЛОЯ

Обязательно проверьте:

### Функциональность
- [ ] Сайт открывается
- [ ] Навигация работает
- [ ] Все страницы доступны
- [ ] Картинки загружаются
- [ ] Шрифты применены

### Производительность
- [ ] Страницы загружаются быстро
- [ ] Нет ошибок в консоли (F12)
- [ ] Мобильная версия работает
- [ ] Картинки оптимизированы

### SEO
- [ ] Заголовки страниц правильные
- [ ] Мета-теги на месте
- [ ] Robots.txt настроен
- [ ] Sitemap.xml создан

### Формы
- [ ] Контактная форма отправляется
- [ ] Калькулятор работает
- [ ] Сообщения приходят в Telegram

---

## 🆘 ОТКАТ ИЗМЕНЕНИЙ

Если что-то пошло не так:

### Через File Manager:
1. Удалить все из `public_html/`
2. Переместить файлы из `backup_old/` обратно

### Через FTP:
1. Загрузить бэкап обратно на сервер

---

## 🚀 ОПТИМИЗАЦИЯ (опционально)

### Кеширование

Уже настроено в `.htaccess`:
- Изображения: 1 год
- CSS/JS: 1 месяц
- HTML: без кеша

### Сжатие (Gzip)

Уже включено в `.htaccess`

### CDN

Рассмотрите использование:
- Cloudflare (бесплатно)
- KeyCDN
- BunnyCDN

---

## 📞 ПОДДЕРЖКА

Если нужна помощь:
1. Проверьте логи ispmanager
2. Проверьте консоль браузера (F12)
3. Проверьте `.htaccess` синтаксис

**Тел:** 8-930-193-34-20

---

## 🎉 ГОТОВО!

Ваш сайт теперь на ispmanager!

**Следующие шаги:**
1. Настроить SSL сертификат
2. Настроить резервное копирование
3. Мониторинг uptime
4. Google Analytics / Яндекс.Метрика

**Удачи! 🚀**

