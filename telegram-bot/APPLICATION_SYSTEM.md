# 📦 СИСТЕМА УПРАВЛЕНИЯ ЗАЯВКАМИ

Полноценная система управления заявками для Telegram бота с персистентным хранилищем и расширенными возможностями.

## 🗂 Структура

```
telegram-bot/
├── bot.js                    # Основной файл бота
├── applicationManager.js     # Модуль управления заявками
├── applications.json         # Хранилище заявок (создается автоматически)
└── applications.backup.json  # Резервная копия
```

## 🔧 Компоненты системы

### 1. ApplicationManager (applicationManager.js)

Центральный модуль для работы с заявками.

#### Основные методы:

```javascript
const applicationManager = require('./applicationManager');

// Инициализация (выполняется автоматически при запуске)
await applicationManager.initialize();

// Создание заявки
const app = await applicationManager.createApplication({
  name: 'Иван Иванов',
  phone: '+79001234567',
  email: 'ivan@example.com',
  message: 'Интересует кухня',
  priority: 'normal' // или 'high'
});

// Получение заявки по ID
const app = applicationManager.getApplication(applicationId);

// Получение всех заявок с фильтрами
const newApps = applicationManager.getAllApplications({ status: 'new' });
const allApps = applicationManager.getAllApplications();

// Обновление статуса
await applicationManager.updateStatus(applicationId, 'done', 'Заказ оформлен');

// Добавление действия
await applicationManager.addAction(applicationId, 'called', {
  note: 'Клиент перезвонит завтра'
});

// Добавление заметки
await applicationManager.addNote(applicationId, 'Важный клиент');

// Удаление заявки
await applicationManager.deleteApplication(applicationId);

// Получение статистики
const stats = applicationManager.getStatistics();

// Очистка старых заявок (старше 30 дней)
await applicationManager.cleanupOldApplications(30);

// Экспорт данных
await applicationManager.exportData();
```

### 2. Структура заявки

```javascript
{
  id: "APP_1699123456789_1",
  name: "Иван Иванов",
  phone: "+79001234567",
  email: "ivan@example.com",
  message: "Интересует кухня",
  calculationData: { /* данные расчета */ },
  status: "new",              // new | work | done
  priority: "normal",         // normal | high
  source: "website",          // website | telegram | phone
  createdAt: "2024-11-05T10:30:00.000Z",
  updatedAt: "2024-11-05T10:30:00.000Z",
  actions: [
    {
      type: "status_change",
      from: "new",
      to: "work",
      timestamp: "2024-11-05T10:35:00.000Z"
    },
    {
      type: "called",
      timestamp: "2024-11-05T10:40:00.000Z"
    }
  ],
  notes: [
    {
      text: "Важный клиент",
      timestamp: "2024-11-05T10:45:00.000Z"
    }
  ]
}
```

### 3. Статусы заявок

- **new** - новая заявка, требует внимания
- **work** - заявка в работе
- **done** - заявка обработана

### 4. Типы действий

- **status_change** - изменение статуса
- **called** - звонок клиенту
- **messaged** - сообщение клиенту
- **note_added** - добавлена заметка

## 🎮 Управление через бота

### Главное меню

```
🌰 ПАНЕЛЬ УПРАВЛЕНИЯ «ЗОЛОТОЙ ДУБ»

[📊 Статистика] [📋 Все заявки]
[⏳ Новые заявки] [✅ Обработанные]
[❓ Помощь]
```

### Кнопки действий с заявкой

```
[✅ Обработано] [⏳ В работе]
[📞 Позвонить] [💬 Написать]
[📝 Заметка] [🗑 Удалить]
```

### Работа с кнопками

#### 1. "Обработано" ✅
- Меняет статус на `done`
- Убирает кнопки управления
- Уведомляет других администраторов

#### 2. "В работе" ⏳
- Меняет статус на `work`
- Показывает, что заявка взята в обработку

#### 3. "Позвонить" 📞
- Показывает телефон клиента для копирования
- Запрашивает подтверждение звонка
- Сохраняет действие в историю

#### 4. "Написать" 💬
- Показывает контакты клиента (телефон, email)
- Запрашивает подтверждение отправки сообщения
- Сохраняет действие в историю

#### 5. "Заметка" 📝
- Добавляет заметку к заявке (в разработке)

#### 6. "Удалить" 🗑
- Запрашивает подтверждение
- Удаляет заявку из системы
- Уведомляет других администраторов

## 📊 Статистика

Система собирает детальную статистику:

```javascript
{
  total: 150,          // Всего заявок
  new: 5,              // Новых
  work: 10,            // В работе
  done: 135,           // Обработано
  called: 120,         // Количество звонков
  messaged: 80,        // Количество сообщений
  highPriority: 15,    // Приоритетных
  today: 8,            // За сегодня
  week: 42             // За неделю
}
```

## 🔄 Уведомления

Система автоматически уведомляет всех администраторов о:

- ✅ Новых заявках с сайта
- ✅ Обработанных заявках
- ✅ Удаленных заявках

## 💾 Хранилище данных

### Файл applications.json

Структура:
```json
{
  "lastId": 42,
  "applications": [ /* массив заявок */ ],
  "updatedAt": "2024-11-05T10:30:00.000Z"
}
```

### Резервное копирование

- Создается автоматически перед каждым сохранением
- Файл: `applications.backup.json`
- Можно восстановить вручную в случае ошибки

### Экспорт данных

```bash
# В боте выполните
await applicationManager.exportData();

# Создастся файл: export_1699123456789.json
```

## 🚀 Запуск системы

```bash
cd telegram-bot
npm install
node bot.js
```

При запуске:
1. Автоматически загружаются существующие заявки из `applications.json`
2. Восстанавливается счетчик ID
3. Создается резервная копия при каждом изменении

## 📝 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота и главное меню |
| `/menu` | Показать главное меню |
| `/stats` | Показать статистику |

## 🔐 Безопасность

- ✅ Доступ только для авторизованных администраторов
- ✅ Подтверждение критичных действий (удаление)
- ✅ Автоматическое резервное копирование
- ✅ Логирование всех действий

## 🧹 Обслуживание

### Очистка старых заявок

```javascript
// Удалить обработанные заявки старше 30 дней
await applicationManager.cleanupOldApplications(30);

// Результат
{
  success: true,
  deletedCount: 15
}
```

### Просмотр истории заявки

```javascript
const app = applicationManager.getApplication(applicationId);

console.log('История действий:', app.actions);
console.log('Заметки:', app.notes);
```

## 🐛 Диагностика

### Проверка состояния

```javascript
// Получить все заявки
const apps = applicationManager.getAllApplications();
console.log(`Всего заявок: ${apps.length}`);

// Статистика
const stats = applicationManager.getStatistics();
console.log('Статистика:', stats);
```

### Восстановление из резервной копии

```bash
# Если основной файл поврежден
cp applications.backup.json applications.json

# Перезапустить бота
node bot.js
```

## 📈 Расширение функционала

### Добавление новых действий

В `applicationManager.js`:
```javascript
await applicationManager.addAction(applicationId, 'custom_action', {
  customField: 'value',
  note: 'Дополнительная информация'
});
```

### Добавление новых фильтров

```javascript
// Фильтр по приоритету
const highPriority = applicationManager.getAllApplications({ 
  priority: 'high' 
});

// Комбинированные фильтры (реализовать самостоятельно)
const newHighPriority = apps.filter(app => 
  app.status === 'new' && app.priority === 'high'
);
```

## 🎯 Рекомендации

1. **Регулярно очищайте старые заявки** для экономии памяти
2. **Делайте экспорт данных** раз в месяц для долгосрочного хранения
3. **Проверяйте резервные копии** после каждого обновления
4. **Мониторьте статистику** для оценки эффективности работы

## 🔄 Интеграция с сайтом

Из Next.js приложения (`lib/telegram.ts`):

```typescript
import { sendTelegramMessage } from '@/lib/telegram';

await sendTelegramMessage({
  id: uniqueId,
  name: formData.name,
  phone: formData.phone,
  email: formData.email,
  message: formData.message,
  formattedMessage: '...',
  source: 'website',
  priority: 'normal'
});
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи бота
2. Убедитесь, что `applications.json` не поврежден
3. Восстановите из резервной копии при необходимости
4. Проверьте права доступа к файлам

---

**Версия:** 1.0.0  
**Дата:** 05.11.2024  
**Статус:** ✅ Готово к использованию

