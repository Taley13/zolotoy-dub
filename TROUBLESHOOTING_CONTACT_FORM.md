# 🔧 Устранение проблемы "Сервис временно недоступен"

## 📋 Что было исправлено

### 1. **Улучшена проверка переменных окружения**
- Добавлена детальная диагностика каждой переменной
- Добавлено логирование типа, длины и значения переменных
- Теперь система точно показывает какие переменные отсутствуют

### 2. **Улучшены сообщения об ошибках**
- Вместо общего "Сервис временно недоступен" теперь показывается:
  - "Временные технические неполадки. Пожалуйста, позвоните нам: 8-930-193-34-20"
- В dev режиме добавляются технические детали для отладки
- Добавлены специфичные сообщения для разных типов ошибок Telegram API

### 3. **Добавлены fallback механизмы**
- Таймаут для HTTP запросов (10 секунд)
- Обработка всех возможных типов ошибок
- Graceful degradation при частичных сбоях

### 4. **Создана утилита проверки окружения**
- `/lib/checkEnv.ts` - проверка всех переменных
- `/app/api/health/route.ts` - health check endpoint
- `test-env-check.js` - скрипт для тестирования

### 5. **Улучшена обработка в ContactForm**
- Корректная обработка новых форматов ошибок
- Показ технических деталей в development режиме
- Более понятные сообщения для пользователей

---

## 🚀 Как исправить проблему

### Шаг 1: Проверьте переменные окружения локально

```bash
# Проверьте что файл .env.local существует
cat .env.local

# Должен содержать:
TELEGRAM_BOT_TOKEN=8397994876:AAHpHKfsdPrEvrGAgIVFGwoOKf6Uw1CPMak
TELEGRAM_CHAT_ID=277767867,956005680
UPSTASH_REDIS_REST_URL=https://healthy-parakeet-35089.upstash.io
UPSTASH_REDIS_REST_TOKEN=AYkRAAIncDFkN2FjYjFkNGJiYjk0OTczOGFmNjM3MThlZTFjY2NhOHAxMzUwODk
```

### Шаг 2: Запустите локальный тест

```bash
# Запустите dev сервер
npm run dev

# В другом терминале - тест
npm run test:env

# Или напрямую
node test-env-check.js
```

Вы должны увидеть:
```
✅ TELEGRAM_BOT_TOKEN: 8397994876...
✅ TELEGRAM_CHAT_ID: 277767867,956005680
✅ UPSTASH_REDIS_REST_URL: https://healthy-parakeet-35089.upstash.io
✅ UPSTASH_REDIS_REST_TOKEN: AYkRAAInc...
```

### Шаг 3: Добавьте переменные в Vercel

Откройте:
```
https://vercel.com/taley13s-projects/zolotoy-dub/settings/environment-variables
```

Добавьте каждую переменную:

| Name | Value | Environment |
|------|-------|-------------|
| `TELEGRAM_BOT_TOKEN` | `8397994876:AAHpHKfsdPrEvrGAgIVFGwoOKf6Uw1CPMak` | Production, Preview, Development |
| `TELEGRAM_CHAT_ID` | `277767867,956005680` | Production, Preview, Development |
| `UPSTASH_REDIS_REST_URL` | `https://healthy-parakeet-35089.upstash.io` | Production, Preview, Development |
| `UPSTASH_REDIS_REST_TOKEN` | `AYkRAAIncDFkN2FjYjFkNGJiYjk0OTczOGFmNjM3MThlZTFjY2NhOHAxMzUwODk` | Production, Preview, Development |

### Шаг 4: Задеплойте проект

```bash
npm run deploy
# или
npx vercel --prod --yes
```

### Шаг 5: Проверьте удаленный сервер

```bash
# Замените URL на ваш домен
npm run test:env:remote https://zolotoy-dub.vercel.app

# Или
node test-env-check.js --remote https://zolotoy-dub.vercel.app
```

### Шаг 6: Проверьте health endpoint

Откройте в браузере:
```
https://ваш-домен.vercel.app/api/health
```

Должно показать:
```json
{
  "status": "healthy",
  "timestamp": "2025-11-06T...",
  "message": "All systems operational"
}
```

Если есть проблемы, добавьте `?debug=true`:
```
https://ваш-домен.vercel.app/api/health?debug=true
```

---

## 🔍 Диагностика проблем

### Проблема: "Временные технические неполадки"

**Причина:** Переменные окружения не установлены в Vercel

**Решение:**
1. Проверьте Vercel Dashboard → Settings → Environment Variables
2. Убедитесь что все 4 переменные добавлены
3. Убедитесь что выбран правильный Environment (Production)
4. Сделайте Redeploy

**Проверка логов:**
```bash
vercel logs --follow
```

Ищите строки:
```
❌ CONFIG ERROR: Missing environment variables
Missing variables: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
```

### Проблема: Форма отправляется но сообщения не приходят

**Причина:** Неверный CHAT_ID или бот заблокирован

**Решение:**
1. Откройте Telegram
2. Найдите вашего бота
3. Нажмите START или отправьте /start
4. Попробуйте снова

**Проверка:**
```bash
# Проверьте что бот отвечает
curl https://api.telegram.org/bot8397994876:AAHpHKfsdPrEvrGAgIVFGwoOKf6Uw1CPMak/getMe
```

### Проблема: Ошибка "Bot was blocked by the user"

**Причина:** Один из получателей заблокировал бота

**Решение:**
1. Попросите получателя разблокировать бота
2. Или удалите этот CHAT_ID из `TELEGRAM_CHAT_ID`

**Система продолжит работать если хотя бы один получатель доступен**

---

## 📊 Мониторинг

### Проверка статуса системы

```bash
# Health check
curl https://ваш-домен.vercel.app/api/health

# Contact API status
curl https://ваш-домен.vercel.app/api/contact

# Telegram webhook status  
curl https://ваш-домен.vercel.app/api/telegram
```

### Просмотр логов в реальном времени

```bash
vercel logs --follow
```

### Тестовая отправка заявки

```bash
curl -X POST https://ваш-домен.vercel.app/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест",
    "phone": "+79001234567",
    "message": "Тестовое сообщение"
  }'
```

Должно вернуть:
```json
{
  "ok": true,
  "message": "Заявка успешно отправлена! Мы свяжемся с вами в ближайшее время.",
  "delivered": 2,
  "failed": 0,
  "total": 2
}
```

---

## 📞 Получение помощи

### Логи показывают ошибку?

1. Скопируйте полный лог ошибки
2. Проверьте раздел "Environment variables diagnostic" в логах
3. Убедитесь что все переменные показывают "✓ YES"

### Всё равно не работает?

1. Проверьте `.env.local` на опечатки
2. Перезапустите dev сервер: `npm run dev`
3. Очистите кеш Next.js: `rm -rf .next`
4. Пересоздайте deployment: `vercel --force`

### Технические контакты

- Telegram для экстренной связи: Напишите вашему боту
- Телефон: 8-930-193-34-20

---

## ✅ Чеклист перед деплоем

- [ ] Все 4 переменные окружения установлены в `.env.local`
- [ ] `npm run dev` запускается без ошибок
- [ ] `npm run test:env` показывает все ✅
- [ ] Тестовая форма отправляется успешно локально
- [ ] Все переменные добавлены в Vercel Dashboard
- [ ] Выбраны все три Environment (Production, Preview, Development)
- [ ] Сделан deploy: `npm run deploy`
- [ ] Health check возвращает "healthy"
- [ ] Тестовая форма работает на продакшене
- [ ] Сообщения приходят в Telegram

---

## 🎉 Готово!

После выполнения всех шагов ваша контактная форма должна работать идеально!

Если проблемы сохраняются - проверьте логи Vercel и используйте `/api/health` для диагностики.

