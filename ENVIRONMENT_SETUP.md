# 🔐 Настройка переменных окружения

## Обязательные переменные

### 1. TELEGRAM_BOT_TOKEN
Токен вашего Telegram бота

**Как получить:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot` (или используйте существующего бота)
3. Скопируйте токен в формате: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

**Значение для вашего проекта:**
```
8397994876:AAHpHKfsdPrEvrGAgIVFGwoOKf6Uw1CPMak
```

---

### 2. TELEGRAM_CHAT_ID
ID чатов для получения уведомлений (через запятую для нескольких получателей)

**Как получить:**
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение боту
3. Скопируйте ваш ID (число)

**Значение для вашего проекта:**
```
277767867,956005680
```

---

### 3. UPSTASH_REDIS_REST_URL
URL для подключения к Upstash Redis (для хранения заявок и сессий)

**Как получить:**
1. Зарегистрируйтесь на [Upstash](https://upstash.com/)
2. Создайте новую Redis базу данных
3. Скопируйте REST URL из Dashboard

**Значение для вашего проекта:**
```
https://healthy-parakeet-35089.upstash.io
```

---

### 4. UPSTASH_REDIS_REST_TOKEN
Токен для аутентификации в Upstash Redis

**Как получить:**
1. В Dashboard вашей Redis базы
2. Найдите раздел "REST API"
3. Скопируйте REST Token

**Значение для вашего проекта:**
```
AYkRAAIncDFkN2FjYjFkNGJiYjk0OTczOGFmNjM3MThlZTFjY2NhOHAxMzUwODk
```

---

## 🚀 Настройка на Vercel

### Шаг 1: Откройте настройки проекта
```
https://vercel.com/taley13s-projects/zolotoy-dub/settings/environment-variables
```

### Шаг 2: Добавьте каждую переменную

Для каждой переменной:
1. Нажмите **"Add New"**
2. **Name**: имя переменной (например, `TELEGRAM_BOT_TOKEN`)
3. **Value**: значение (скопируйте из списка выше)
4. **Environment**: выберите **ALL** (Production, Preview, Development)
5. Нажмите **"Save"**

### Шаг 3: Redeploy проекта

После добавления всех переменных:
```bash
npx vercel --prod
```

Или через Dashboard Vercel:
1. Deployments → Latest Deployment
2. Нажмите на три точки (⋯)
3. Выберите "Redeploy"

---

## 💻 Локальная разработка

Создайте файл `.env.local` в корне проекта:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=8397994876:AAHpHKfsdPrEvrGAgIVFGwoOKf6Uw1CPMak
TELEGRAM_CHAT_ID=277767867,956005680

# Upstash Redis Configuration
UPSTASH_REDIS_REST_URL=https://healthy-parakeet-35089.upstash.io
UPSTASH_REDIS_REST_TOKEN=AYkRAAIncDFkN2FjYjFkNGJiYjk0OTczOGFmNjM3MThlZTFjY2NhOHAxMzUwODk
```

⚠️ **Важно:** Файл `.env.local` не должен коммититься в Git (он уже в `.gitignore`)

---

## 🔍 Проверка настройки

### 1. Проверить endpoint контактной формы
```bash
curl https://ваш-домен.vercel.app/api/contact
```

Должно вернуть:
```json
{
  "status": "ok",
  "message": "Contact API endpoint is working"
}
```

### 2. Проверить Telegram webhook
```bash
curl https://ваш-домен.vercel.app/api/telegram
```

Должно вернуть:
```json
{
  "status": "ok",
  "message": "Telegram webhook endpoint is ready"
}
```

### 3. Проверить логи на Vercel

После отправки тестовой заявки:
```bash
vercel logs
```

Ищите строки:
- `✅ All required environment variables are present`
- `✅ Successfully sent to [chat_id]`

---

## 🐛 Решение проблем

### Ошибка: "Сервис временно недоступен"

**Причина:** Переменные окружения не установлены или не загружаются

**Решение:**
1. Проверьте что все 4 переменные добавлены в Vercel
2. Убедитесь что переменные назначены на правильный environment (Production)
3. Сделайте Redeploy проекта
4. Проверьте логи: `vercel logs`

### Ошибка: "Bot was blocked by the user"

**Причина:** Бот был заблокирован пользователем в Telegram

**Решение:**
1. Найдите вашего бота в Telegram
2. Нажмите "START" или "/start"
3. Попробуйте отправить заявку снова

### Ошибка: "Unauthorized"

**Причина:** Неверный токен бота

**Решение:**
1. Проверьте правильность `TELEGRAM_BOT_TOKEN`
2. Получите новый токен через @BotFather если нужно
3. Обновите переменную в Vercel
4. Сделайте Redeploy

---

## 📞 Контакты

Если проблемы сохраняются:
- Проверьте логи Vercel
- Проверьте логи в браузере (DevTools → Network → api/contact)
- Свяжитесь с технической поддержкой

**Телефон для экстренной связи:** 8-930-193-34-20

