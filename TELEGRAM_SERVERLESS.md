# 🚀 SERVERLESS TELEGRAM БОТ НА VERCEL

Документация по serverless версии Telegram бота на Next.js API Routes.

## 🎯 Концепция

Вместо постоянно работающего Node.js процесса (polling), бот работает через **вебхуки** в serverless среде Vercel.

### Преимущества serverless:
- ✅ **Работает 24/7 автоматически** - не нужно держать сервер включенным
- ✅ **Масштабируется автоматически** - Vercel управляет нагрузкой
- ✅ **Бесплатно на Hobby плане** - до 100GB bandwidth
- ✅ **Быстрее отвечает** - вебхуки быстрее polling
- ✅ **Меньше ресурсов** - платите только за использование

### Отличия от polling версии:

| Функция | Polling (bot.js) | Webhook (serverless) |
|---------|------------------|----------------------|
| Режим работы | Постоянный процесс | По запросу |
| Хранилище сессий | Map() в памяти | Vercel KV (Redis) |
| Хранилище заявок | Файл applications.json | Vercel KV или Postgres |
| Деплой | VPS/PM2 | Vercel автоматически |
| Масштабирование | Ручное | Автоматическое |
| Стоимость | Сервер 24/7 | Pay-per-use |

---

## 📁 Структура файлов

```
app/
├── api/
│   └── telegram/
│       ├── route.ts                 # Главный обработчик вебхука
│       ├── webhook-info/
│       │   └── route.ts            # Получение статуса вебхука
│       ├── set-webhook/
│       │   └── route.ts            # Установка вебхука
│       ├── delete-webhook/
│       │   └── route.ts            # Удаление вебхука
│       └── test-message/
│           └── route.ts            # Тестовое сообщение
└── admin/
    └── telegram-webhook/
        └── page.tsx                 # Админская страница управления
```

---

## 🔧 Настройка

### 1. Установка зависимостей

```bash
npm install node-telegram-bot-api @vercel/kv
npm install -D @types/node-telegram-bot-api
```

Уже добавлены в `package.json`.

### 2. Настройка переменных окружения

В `.env.local`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=277767867,956005680
```

В **Vercel Dashboard** → Settings → Environment Variables добавьте те же переменные.

### 3. Настройка Vercel KV (для сессий)

1. Зайдите в Vercel Dashboard
2. Выберите проект → Storage → Create Database
3. Выберите **KV (Redis)**
4. Vercel автоматически добавит переменные `KV_*`

**Или** используйте локальную Redis для разработки.

### 4. Деплой на Vercel

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel --prod
```

### 5. Установка вебхука

После деплоя откройте:

```
https://zol-dub.online/admin/telegram-webhook
```

Нажмите **"Установить Webhook"** и проверьте статус.

---

## 🛠 API Routes

### POST /api/telegram

Главный обработчик вебхуков от Telegram.

**Что обрабатывает:**
- Команды: `/start`, `/menu`
- Текстовые сообщения (для форм)
- Callback queries (кнопки)

**Пример запроса от Telegram:**

```json
{
  "update_id": 123456789,
  "message": {
    "message_id": 1,
    "from": {
      "id": 277767867,
      "first_name": "Иван"
    },
    "chat": {
      "id": 277767867,
      "first_name": "Иван",
      "type": "private"
    },
    "text": "/start"
  }
}
```

### GET /api/telegram

Проверка работоспособности endpoint.

**Ответ:**
```json
{
  "status": "ok",
  "message": "Telegram webhook endpoint is ready",
  "timestamp": "2024-11-05T14:30:00.000Z"
}
```

### GET /api/telegram/webhook-info

Получение информации о текущем вебхуке.

**Ответ:**
```json
{
  "ok": true,
  "result": {
    "url": "https://zol-dub.online/api/telegram",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### POST /api/telegram/set-webhook

Установка URL вебхука.

**Тело запроса:**
```json
{
  "url": "https://zol-dub.online/api/telegram"
}
```

### POST /api/telegram/delete-webhook

Удаление вебхука.

### POST /api/telegram/test-message

Отправка тестового сообщения.

**Тело запроса:**
```json
{
  "chatId": "277767867"
}
```

---

## 💾 Хранилище данных

### Vercel KV (Redis)

Используется для хранения **временных сессий пользователей**.

#### Структура данных:

```typescript
// Сессия пользователя
{
  type: 'measurement' | 'consultation',
  step: 'name' | 'phone',
  name?: string,
  phone?: string
}
```

#### API:

```typescript
import { kv } from '@vercel/kv';

// Сохранить сессию (TTL 30 минут)
await kv.set(`session:${chatId}`, session, { ex: 1800 });

// Получить сессию
const session = await kv.get<UserSession>(`session:${chatId}`);

// Удалить сессию
await kv.del(`session:${chatId}`);
```

### Заявки

В текущей реализации заявки также сохраняются в KV:

```typescript
// Сохранить заявку
await kv.set(`application:${applicationId}`, application);

// Добавить в список
await kv.lpush('applications:all', applicationId);
```

**Альтернативно** можно использовать:
- Vercel Postgres (для реляционных данных)
- Supabase
- MongoDB Atlas

---

## 🔄 Миграция с polling на webhook

### Шаг 1: Остановите старый бот

```bash
# Если использовали PM2
pm2 stop telegram-bot
pm2 delete telegram-bot

# Или просто Ctrl+C
```

⚠️ **Важно:** Нельзя одновременно использовать polling и webhook!

### Шаг 2: Задеплойте на Vercel

```bash
vercel --prod
```

### Шаг 3: Установите вебхук

Откройте `/admin/telegram-webhook` и нажмите "Установить Webhook".

### Шаг 4: Проверьте

1. Отправьте боту `/start`
2. Проверьте логи в Vercel Dashboard
3. Создайте тестовую заявку

### Шаг 5: Миграция данных (если нужно)

Если у вас были заявки в `applications.json`, импортируйте их в KV:

```typescript
// Скрипт миграции (запустить локально)
import { kv } from '@vercel/kv';
import fs from 'fs';

const data = JSON.parse(fs.readFileSync('applications.json', 'utf8'));

for (const app of data.applications) {
  await kv.set(`application:${app.id}`, app);
  await kv.lpush('applications:all', app.id);
}
```

---

## 🧪 Тестирование

### Локальная разработка

```bash
# Запустите dev сервер
npm run dev

# Используйте ngrok для туннеля
npx ngrok http 3000

# URL будет: https://abc123.ngrok.io
# Установите вебхук на этот URL
```

### Production тестирование

1. Откройте `/admin/telegram-webhook`
2. Проверьте статус вебхука
3. Отправьте тестовое сообщение
4. Отправьте боту `/start` в Telegram
5. Создайте заявку через бота

### Проверка логов

В Vercel Dashboard:
1. Выберите проект
2. Deployments → Latest
3. View Function Logs
4. Смотрите логи в реальном времени

---

## 🐛 Решение проблем

### Вебхук не устанавливается

**Проблема:** Ошибка "Bad Request: bad webhook: HTTPS url must be provided"

**Решение:**
- Убедитесь, что URL начинается с `https://`
- Vercel автоматически предоставляет HTTPS

### Бот не отвечает

**Проблема:** Бот не реагирует на сообщения

**Проверьте:**
1. Статус вебхука в `/admin/telegram-webhook` (должен быть 🟢 Активен)
2. Логи в Vercel Dashboard
3. URL вебхука правильный
4. Старый бот (polling) остановлен

### Ошибка KV_REST_API_URL not found

**Проблема:** Не настроен Vercel KV

**Решение:**
1. Создайте KV database в Vercel Dashboard
2. Добавьте переменные окружения
3. Передеплойте проект

### Сессии не сохраняются

**Проблема:** Форма не работает, данные теряются

**Проверьте:**
- KV настроен и доступен
- Переменные окружения `KV_*` установлены
- Нет ошибок в логах

### Админы не получают уведомления

**Проблема:** Заявки создаются, но уведомления не приходят

**Проверьте:**
- `TELEGRAM_CHAT_ID` правильно установлен
- Админы хотя бы раз писали боту
- Логи на наличие ошибок отправки

---

## 📊 Мониторинг

### Vercel Analytics

Автоматически доступно в Vercel Dashboard:
- Количество запросов
- Время выполнения
- Ошибки
- Использование bandwidth

### Логирование

В коде используется `console.log()` - логи видны в Vercel Dashboard.

**Пример логов:**
```
[Telegram API] Получен вебхук: {...}
[Telegram API] ✅ Создана публичная заявка APP_... от Иван
```

### Алерты

Настройте уведомления в Vercel:
- Dashboard → Settings → Notifications
- Уведомления об ошибках
- Уведомления о деплоях

---

## 💰 Стоимость

### Vercel Hobby (бесплатно)

- ✅ 100 GB bandwidth
- ✅ 100,000 Function Invocations
- ✅ 1,000 Serverless Function Hours
- ✅ KV Database: 30,000 команд/день

**Достаточно для:**
- ~10,000 сообщений боту в день
- ~1,000 создаваемых заявок в день

### Vercel Pro ($20/месяц)

- Больше bandwidth и invocations
- Priority support
- Больше KV команд

---

## 🔐 Безопасность

### Проверка запросов от Telegram

Добавьте в `route.ts`:

```typescript
// Проверка secret token
const SECRET_TOKEN = process.env.TELEGRAM_WEBHOOK_SECRET;

export async function POST(req: NextRequest) {
  const token = req.headers.get('X-Telegram-Bot-Api-Secret-Token');
  
  if (SECRET_TOKEN && token !== SECRET_TOKEN) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  // ... остальной код
}
```

При установке вебхука:

```typescript
await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/setWebhook`, {
  method: 'POST',
  body: JSON.stringify({
    url: webhookUrl,
    secret_token: SECRET_TOKEN
  })
});
```

### Rate limiting

Vercel автоматически защищает от DDoS, но можно добавить:

```typescript
import { kv } from '@vercel/kv';

// Проверка rate limit
const key = `rate:${chatId}`;
const count = await kv.incr(key);
await kv.expire(key, 60); // 60 секунд

if (count > 20) {
  return NextResponse.json({ error: 'Too many requests' }, { status: 429 });
}
```

---

## 📈 Оптимизация

### Кэширование

```typescript
export const runtime = 'edge'; // Используйте Edge Runtime для скорости

export const dynamic = 'force-dynamic'; // Не кэшировать ответы
```

### Холодный старт

Serverless функции могут иметь "холодный старт" (100-500ms).

**Решение:** Vercel Edge Functions стартуют за ~5ms.

### Батчинг уведомлений

Отправляйте уведомления админам параллельно:

```typescript
await Promise.all(
  ADMIN_IDS.map(adminId => 
    bot.sendMessage(adminId, text, options)
  )
);
```

---

## 🎯 Roadmap

### Ближайшие планы

- [ ] Полная интеграция с applicationManager
- [ ] Миграция на Vercel Postgres для заявок
- [ ] Админские функции (статистика, списки)
- [ ] Webhooks для обновления заявок
- [ ] Уведомления о новых заявках с сайта

### Будущее

- [ ] Analytics dashboard
- [ ] Экспорт заявок в CRM
- [ ] Автоответы клиентам
- [ ] Интеграция с email

---

## 📚 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel KV](https://vercel.com/docs/storage/vercel-kv)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Webhooks](https://core.telegram.org/bots/webhooks)
- [Next.js API Routes](https://nextjs.org/docs/app/building-your-application/routing/route-handlers)

---

## 🆘 Поддержка

### Проблемы с деплоем

1. Проверьте логи: Vercel Dashboard → Deployments
2. Проверьте переменные окружения
3. Убедитесь, что все зависимости установлены

### Вопросы по функционалу

См. документацию:
- [PUBLIC_ACCESS.md](telegram-bot/PUBLIC_ACCESS.md) - публичный функционал
- [APPLICATION_SYSTEM.md](telegram-bot/APPLICATION_SYSTEM.md) - система заявок

---

**Версия:** 3.0.0 (Serverless)  
**Дата:** 05.11.2024  
**Статус:** ✅ Готово к деплою

После деплоя на Vercel бот будет работать автоматически 24/7! 🚀

